server:
  servlet:
    context-path: /bpm

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin

logging:
  config: "classpath:logging/log4j2-test.xml"

application:
  jtt:
    protocol: "http"
    host: "localhost"
    port: "8080"
    context: "swp-jtt"
